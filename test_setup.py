import sys
import sqlite3
import requests
import schedule
from dotenv import load_dotenv
import yaml
import moviepy

print(f"Python version: {sys.version}")
print(f"Python executable: {sys.executable}")
print("✓ All packages imported successfully!")
print("✓ Your Windows 10 Python environment is ready!")